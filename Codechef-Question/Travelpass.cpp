#include <bits/stdc++.h>
#include <algorithm>
using namespace std;
int main()
{
    int t;
    int s[100];
    int length;
    int Amin, Bmin;
    cin >> t;
    while (t--)
    {

        long long unsigned int n;
        cin >> n;
        string *numbers = new string[n];
        for (size_t i = 0; i < n; i++)
        {
            cin >> numbers[i];
        }

        sort(s, ns + n);

    }
    return 0;
}